#include<bits/stdc++.h>

using namespace std;

int maxFrequency(vector<int>& nums, int k) {
        
}

int main(){
    vector<int> v = {1,4,8,13};
    int k = 5;
    cout << endl << maxFrequency(v, k);
}